from setuptools import setup, find_packages

setup(
    name='segment_tumour',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add required dependencies here
    ],
)